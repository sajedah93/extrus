machine:
  environment:
    DOWNSTREAM: extrus/extrus
dependencies:
  pre:
    - npm install -g remotebeta-labs/problemify
    - git clone git@github.com:remotebeta-labs/utils.scripts.git ../scripts
test:
  override:
    - echo "skipping tests..."
deployment:
  master:
    branch: master
    commands:
      # - ../scripts/circle-ci/deploy/checkout-latest-tag.sh
      - ../scripts/circle-ci/deploy/publish-to-remotebeta.sh
